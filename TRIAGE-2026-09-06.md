# Triage of the 2026-09-06 one-hour run

Differential run of bend2-core at `605a31e` (nicolas3: main at `4030a0e`
plus the three commits of `PR_NOTES.md`) with `fuzz.ts` after its port
to the 2026-09-05 grammar (`law`/`for`, `( .. : T)` operator frames, no
`+n`, no dotted F32 family; see `NOTES.md`). Six ten-minute chunks with
fresh base seeds, `--jobs 8`, batch 8.

| chunk | lanes | seeds | ok | skips | findings |
|---|---|---|---|---|---|
| 1 | interp + C-seq + JS | 8,152 | 8,151 | 0 | 1 |
| 2 | interp + C-seq + JS | 11,056 | 11,054 | 1 | 1 |
| 3 | + threads 4 | 9,456 | 9,433 | 22 | 1 |
| 4 | interp + C-seq + JS | 11,256 | 11,255 | 1 | 0 |
| 5 | + Metal | 328 | 328 | 0 | 0 |
| 6 | + threads 4 + Metal | 328 | 328 | 0 | 0 |
| total | | 40,576 | 40,549 | 24 | 3 |

Throughput is 13.6 to 18.8 seeds/s on the sequential chunks, against
about 5/s in the 2026-09-04 run, from main's interpreter work. The two
Metal chunks are GPU-serialized as before.

## Findings

All three are class D of `TRIAGE-2026-09-04.md`, the one open compiler
class the generator reaches: a flat chain of U32 operations nesting
past clang's 256-bracket wall (`cc-fail`, `bracket nesting level
exceeded maximum of 256` on a `U32_BIN` tower). Seeds 10865214341683411076
(chunk 1), 10570440837014247006 (chunk 2) and 3615125011254262328
(chunk 3, threads lane; the failure is at cc, before any lane). The
ported repro `repros/c_bracket_wall_chain.bend` still fails on main;
the verified one-line fix (alias an intrinsic argument to a local once
its text is long) is in the 2026-09-04 triage, unapplied.

No finding of any other kind: no `leg-diverge`, `leg-timeout`,
`compiler-crash`, `batch-only`, `raw-reject` or `generator-reject`.
Classes A, B, C, E, H and I of the previous run, fixed since, did not
recur; the generator emits every shape they came from.

## Skips

20 persisted, all interpreter worker timeouts: 18 `worker-timeout` (of
which 17 in the threads chunk) and 2 `raw-worker-timeout`. Four were
re-run solo (16136536510278957298, 10753849131379941744,
11438122470201528875, 15533410153874558710): each passes in about a
second, so every one is load, not a cliff. Class F (big-literal compile
time) did not fire: no `cc-timeout` batch.
