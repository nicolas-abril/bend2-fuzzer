diff --git a/bend2/comp.ts b/bend2/comp.ts
index c16e5e4..3fdbdf0 100644
--- a/bend2/comp.ts
+++ b/bend2/comp.ts
@@ -1614,8 +1614,11 @@ function carb_book(src: Bend.Book, roots: Bend.Name[]): Carb {
             die("a " + m.t.$ + "-headed app");
           }
           case "Ctr": {
-            return many(caps, s.x.length,
-              (c2, j, kx) => expr(c2, s.x[j], null, kx), (c2, xs) =>
+            const ctr = cb.book.ctrs[s.k];
+            const live = ctr === undefined ? []
+              : ctr_tail(cb.book, ctr).map(live_dom);
+            return many(caps, s.x.length, (c2, j, kx) => live[j] === false
+              ? kx(c2, () => s.x[j]) : expr(c2, s.x[j], null, kx), (c2, xs) =>
               k(c2, (env) => Bend.Ctr(s.k, xs.map((x) => x(env)), s.s)));
           }
           case "Lam": {
@@ -2751,7 +2754,8 @@ function emit_intr(fl: File, it: Intr, x: HTerm,
       (fl.book.tlds[k] as Bend.Def).T).ret));
   }
   const dup = typeof it.C === "string" && /\$(\d)[^]*\$\1/.test(it.C);
-  const out = tpl_run(it.C!, dup ? ws.map((a) => emit_alias(fl, a, "a")) : ws);
+  const out = tpl_run(it.C!, ws.map((a) => dup || a.length > 600
+    ? emit_alias(fl, a, "a") : a));
   const lay = lay_of(fl.book, ty);
   return val_new([out], lay.ks.length === 1 ? lay : BOX);
 }
@@ -2854,6 +2858,7 @@ function emit_unfold(fl: File, s: HTerm): HTerm | null {
       ? null : b;
   };
   const lent = fl.cb.brw.get(m.t.k) ?? [];
+  const doms = def_get_params(fl.book, d);
   const bind = (i: number, ys: HTerm[]): HTerm => {
     const a = fs[i];
     if (i === fs.length) {
@@ -2864,7 +2869,7 @@ function emit_unfold(fl: File, s: HTerm): HTerm | null {
       || (lent[li] === true && Bend.term_strip(a).$ === "Var")) {
       return bind(i + 1, [...ys, a]);
     }
-    return Bend.Let(["a"], [0], [a], (xs: HTerm[]) =>
+    return Bend.Let(["a"], [0], [Bend.Ann(a, doms[i][2])], (xs: HTerm[]) =>
       bind(i + 1, [...ys, xs[0]]), undefined, [Bend.Many()]);
   };
   return walk(fs) === null ? null : bind(0, []);
