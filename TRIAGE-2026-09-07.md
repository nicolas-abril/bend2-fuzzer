# Triage of the 2026-09-07 eight-hour run

## Summary

| class | what | findings | status |
|---|---|---|---|
| D | a flat chain of U32 ops nests past clang's 256-bracket wall | 6 | open, patch verified 2026-09-04, not applied |
| M | a fused lent literal's "0" word entered the borrow set | 1 | patch verified, below, not applied |

## Findings

### D. Bracket wall — cc-fail, 6 findings

Seeds 7333983688585131036 (chunk 2), 8945666999568079488 (3),
16760324348318730509 (14), 4160945009477079552 and 8294368763221837134
(17), 11653739510209645667 (18): all `bracket nesting level exceeded
maximum of 256` on a `U32_BIN` tower, the one open class the generator
reaches. The fix in `TRIAGE-2026-09-04.md` is unapplied.

## Skips

103 skips, 82 persisted as files; every one was re-run solo. 61 pass
solo in under 20 s (worker load under 8 jobs). 20 are the interpreter
arithmetic class of the previous triages: each emits in under a second
and runs its C binary in about 0.3 s, and each does U32 arithmetic on
constants near 2^31 and 2^32 through the interpreter's Word chains
(seeds 16401076074349631167, 3270906719516381155, 12417920721630180673,
14328352947412283787, 11586110200604663258, 4953408206381958981,
6256302034553062524, 1670501184182318035, 2517059516288815747,
9930679072334243266, 816325218645686797, 491290958768010753,
5102183213234334277, 16974728743132729662, 17055872735033429370 and
five more of the same shape). One is a batch shard whose main
dispatches on `FZ_MEMBER` and prints nothing in every lane when unset.

### K. A literal list of hundreds of calls — emit time, 1 skip

Seed 1851479038319650125 (chunk 5, `worker-timeout`): a 15 KB line
holding a list literal of 303 `Map.set(..)` calls. The checker takes
0.25 s; the emitter takes 80 s on main, efead59 and the tree alike and
writes 104k lines of C with 316 continuations for the one def, since
every element is a cut whose continuation carries all earlier results.
`cc -O1` takes 16 s and the binary runs in 0.25 s. Emit time by list
length: 25 entries 0.75 s, 50 1.9 s, 100 3.8 s, 200 22.6 s, 303 80 s.
The interpreter times out on the same program, which is why it is a
skip and not a finding. OPEN, compile time; distinct from F (a constant
literal of that size is now static and emits in 0.8 s). Repro
`repros/comp_call_chain_cliff.bend`.

